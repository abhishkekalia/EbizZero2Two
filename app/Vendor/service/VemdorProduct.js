import React, { Component } from 'react';
import { 
    Platform,
    Dimensions
} from 'react-native';

let VemdorProduct = {
    findAll: function(sortBy) {
        return repository;
    },
};

module.exports = VemdorProduct;